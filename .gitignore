.DS_Store
.claude/
storageState.json
storageState*.json
*-session/cookies.json
*-session/storage.json

# 一次性数据备份(不入库)
*_backup_*.json

# res_api 调试草稿：一次性排查脚本与截图，随手产生、不入库。
# deploy.sh 已用 --exclude "/_*" 把它们排除在部署之外，这里同步排除出 git，
# 免得 163 个文件把 git status 淹掉、让真正的改动看不见。
# （前导斜杠是 2026-08-01 补的：原来的 "_*" 无锚点、会匹配任意层级，
#   把 bakery-ops/src/app/api/import/_auth.ts 也一起排除掉了。）
res_api/_*

# 生产库备份：pg_dump 全量转储，含全部业务数据。只在本机与异地存储保留，绝不入 git。
backups/

# Python 生成缓存与 Supabase CLI 本地状态：可再生，不入库
__pycache__/
.venv/
.pytest_cache/
.ruff_cache/
supabase/.temp/
supabase/.branches/

# output/ 下是含员工姓名与薪资的分析报告 PDF：个人数据只在本机交付，不进 git 历史
output/
