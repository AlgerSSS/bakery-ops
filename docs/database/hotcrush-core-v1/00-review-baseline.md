# 00 评审证据基线

冻结本轮方案 C 使用的数据库快照、代码扫描与 Git 边界；避免把不同时间点混成同一事实。

> 状态：**方案 C 评审稿，不是迁移脚本，不授权修改生产数据库。**
> 模型版本：`HOTCRUSH-CORE-V1-REVIEW-R6-2026-08-10`；生产结构快照：`2026-08-09T09:56:05.204Z`；代码静态扫描：`2026-08-09T10:30:31.506548+00:00`。

## 已确认事实

- 数据库结构快照采集于 `2026-08-09T09:56:05.204Z`，连接角色 `postgres`，包含 76 表、21 视图和 939 列。
- 代码访问快照采集于 `2026-08-09T10:30:31.506548+00:00`；扫描后只有运行源码/脚本层才进入项目读写矩阵，文档、测试、迁移和 JSON 数据不作为运行时调用证明。
- 本轮生成时仓库 HEAD 为 `6368c079b14a62ebdd0f93f7b3c12178370f135c`，分支 `codex/r6-green-implementation`，工作区不干净。
- 当前未跟踪的迁移 `109_margin_and_holiday_factor.sql` 声明视图 `v_daily_margin`, `v_holiday_factor`, `v_item_cost_quality`；其中数据库快照也存在 `v_daily_margin`, `v_holiday_factor`, `v_item_cost_quality`。这是对象重合证据，不是部署来源证明。

## 合理推测

- 因数据库快照时间、代码扫描时间和当前脏工作区不是同一个不可变提交，本包适合结构评审和迁移清单，不足以单独证明某一生产接口的当前行为。
- 静态读写线索可用于定位契约所有者，但上线前仍需生产日志、部署提交、连接角色和契约测试补证。

## 暂时无法验证

- BakeryOps、RES/POS、财务网站、HBTI 各生产部署实际运行提交及所有动态 SQL。
- 外部 Excel、人工 SQL、定时脚本和未在本地目录中的客户端是否仍直接写库。
- 当前未跟踪 109 文件与数据库内同名视图之间的确切部署链路。

完整机器可读清单见 `evidence/review-baseline.json`。
