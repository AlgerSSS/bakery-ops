# SUPERSEDED — HOT CRUSH Core V1 R6 历史验收记录

> **本文件已被取代，不再代表当前模型或当前计数。** 当前证据请读
> [`p0c-source-fidelity-and-reward-2026-08-10.md`](p0c-source-fidelity-and-reward-2026-08-10.md)。
> 下文原数字与原hash仅保留为历史，不得用于实施或验收。

验收日期：2026-08-10（Asia/Kuala_Lumpur）

## 结论

**设计评审包通过交付门禁。** 这证明目标数据库蓝图、逐表逐字段字典、现库迁移去向、关系图和实施门禁在本次冻结证据范围内完整且内部一致；不代表生产迁移已经执行，也不代表尚未实测的动态 SQL、真实 RLS/JWT 或四个部署目标已完成切换。

## 最终结构

- 原 R5 的 154 项是“候选对象处置数”，不是 R6 表数量。
- R6 共 137 张潜在物理表：一期 100 张（81 张业务事实/身份/工作流 + 19 张必要平台状态），扩展包 33 张，来源满足条件后再启用 4 张。
- 另有 59 个只读派生视图：一期 41 个、扩展 13 个、来源条件 5 个。
- 154 项去向严格守恒：137 张物理契约 + 11 项合并 + 4 项派生 + 2 项删除 = 154。

## 表、字段和关系完整性

- 137/137 张物理表和 59/59 个视图均有明确用途、单行粒度、写入者、读取者、来源、生命周期、变更策略和物理存储理由。
- 1,810/1,810 个物理字段和 642/642 个视图字段均有中文名称、类型、空值、默认值、存放内容、业务作用、来源/写入方、约束、时间语义、版本语义、敏感性、示例和误用警告。
- `target-comments-contract.sql` 恰好包含 196 条对象注释和 2,452 条字段注释，无空注释、未知对象或重复语句。
- 419 个外键字段全部进入图源关系审计；61 页 Draw.io 覆盖全部 137 张表、59 个视图和 15 条端到端链路。
- 当前生产快照的 939 个字段在迁移矩阵中恰好出现一次；高风险对象强制逐字段映射，不允许对象级兜底或静默丢弃。

## 关键语义门禁

- 可选来源数值保留 NULL，不以默认 0 伪造事实；`hr_timesheet_entry.break_minutes` 已采用 NULL 安全规则。
- 来源客流和来源平均客单价与可派生的标准指标分离。
- 全部 56 个 JSONB 字段被穷尽划分为 17 个行为驱动字段和 39 个证据字段，集合互斥；行为驱动字段全部具有版本化数据库校验门禁。
- 消息订阅保存在 `app_user.notification_subscription_codes` 这一不可确定重建的当前偏好中，不新增猜测性表；更新必须经过代码注册表校验并写审计事件。
- 派生毛利、成本快照、当前态、汇总和核对结果均保持为只读视图，不冒充原子事实表。

## 自动验收证据

`python3 tools/validate-review-package.py`：通过，零模型警告。

- 表 137；视图 59；字段 2,452；外键 419。
- 字段注释 2,452；对象注释 196。
- 当前字段映射 939；R5 去向 154；现库门禁映射 467。
- Draw.io 61 页；PDF 61 页；高清 PNG 4 张。

生成器和 Draw.io 源图连续重跑两次后的 52 文件确定性聚合 SHA-256 完全一致：

`a25ea975678e99f41ee532d7c35282f1613b226e26e5a0426354f0213f51f057`

该哈希不包含第三方导出的 PNG/PDF/交互 HTML，因为它们可能带导出元数据；这不是隐瞒，而是明确边界。视觉资产另行通过页数、覆盖、分辨率、文件摘要和人工可读性检查。

## 视觉验收

- 总览 PNG：6000 × 3690；无裁切、无文字框重叠、关系线可辨识。
- 会员订单 PNG：6000 × 1606；无裁切、表/视图/跨域引用可辨识。
- 采购订单 PNG：6000 × 2374；无裁切、版本/行/引用关系可辨识。
- 派生成本 PNG：6000 × 3810；无裁切、物理来源与派生视图边界可辨识。
- PDF：61 页，4,938,221 bytes。

## 独立审计

- Claude Fable 5 完整审计：`PASS`；独立复算结构、字段、注释、映射、JSON 分类、419 个连接和视觉资产。
- Claude 唯一提出的非阻断旧文案已修正。
- 修正后的聚焦复核再次返回 `PASS`，确认新哈希、全部计数及 M1/M2/M3 关闭状态。

## 未越权执行的边界

- 未执行任何生产数据库 DDL/DML。
- 未生成或应用生产迁移。
- 未修改四个项目的数据库读写契约。
- 未部署、未提交 Git。
- 后续只有在用户批准蓝图后，才能进入迁移文件、回填、双轨核对、切换和回滚阶段。
