# 生日礼预约通知 relay 的服务器配置（实际文件在 /opt/hotcrush/scripts/birthday-notify.env，不入 git）
# 与 res_api/.env 里的 DATABASE_URL 同一生产库（pgbouncer :6543 入口）。
DATABASE_URL=
# Lark「HOT CRUSH 生日礼预约」群（2026-08-15 由机器人创建）
BIRTHDAY_NOTIFY_CHAT_ID=
