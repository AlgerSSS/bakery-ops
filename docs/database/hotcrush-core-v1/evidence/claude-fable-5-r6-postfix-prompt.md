# HOT CRUSH Core V1 R6 修订后独立终审任务

你是独立数据库架构审计者。只读审计 `/Users/weiliangshao/hot/docs/database/hotcrush-core-v1`；不要修改任何文件，不要执行 DDL/DML、部署或提交。必须使用第一性原则，不要因生成器或验证器显示绿色就默认通过。

## 真正目标

HOT CRUSH 只服务一个企业，但支持多门店、厨房、仓库与未来模块。数据库应优先保存不可由其他稳定事实确定性重建的最小事实、稳定身份、人工决定、外部副作用和恢复/安全状态；比率、差值、排名、画像、成本与毛利等可重算结果优先作为只读视图。各模块必须通过稳定 UUID、营业日、来源、批次/版本和生效区间连接，不能依赖名称或无约束多态文本。

## 背景与修订

你上一次完整审计保存在 `evidence/claude-fable-5-r6-final-138-review.md`，结论为 `PASS_WITH_CHANGES`。不要照抄上次结论，必须核对当前文件。上次指出的 A1/A2/A3 及字段、实施门禁问题声称已修订，至少包括：

1. 154 项原始 Claude 判定被声明为不可改写的映射，CSV 新增 `final_override`，验证器会与原始 154 行证据交叉核对。
2. POS 会员主档、卡交易和会员日报补回来源不可重建字段；会员余额快照改为当前证据实际支持的 member × date × batch，而不是虚构 card 粒度。
3. 53 个视图递归计算物理表闭包并分为 PHASE1 / EXTENSION_PACK / SOURCE_CONDITIONAL；身份映射缺口视图不再依赖扩展表。
4. `pos_sales_hour.unassigned_order_count` 不再物理保存；商品状态统一为 `PRODUCT_MAPPING_PENDING`；覆盖范围统一为 `MEMBER_FLAGGED_ONLY`；消息表移除 `extension_source_type/id`。
5. 店长客单价增加独立来源字段门禁；采购单合计增加核对约束；收货行与采购版本一致性声明为延迟约束触发器门禁。
6. 当前 467 个约束/索引/触发器/RLS 均有迁移处置矩阵；138 张潜在物理表各有 PK、唯一约束、FK 索引候选、行访问、写入/冻结、删除和特殊约束门禁。
7. 生成器声称输出 138 张潜在物理契约，其中首期仅 100（81 业务 + 19 平台侧车），34 张扩展、4 张来源条件；另有 11 合并、4 派生、1 删除。不得把 138 或 154 当作首期建表数。
8. 目标目录声称 1762 个物理字段、522 个视图字段、2284 个字段注释，138 表 + 53 视图共 191 个对象注释，418 个外键；Draw.io 与 PDF 均为 61 页。

## 必须独立检查

1. 重新计数并交叉核对表、视图、字段、注释、外键、154 去向、视图分期、实施门禁和 61 页，不要只引用验证器输出。
2. 对 138 张潜在物理表逐张判断：首期/扩展/来源条件是否合理；是否仍有能从稳定基础事实确定性派生却被物理保存的表；是否有粒度混合、重复主数据、伪事实或没有明确写入者的首期表。
3. 对全部物理字段做可派生性与保真扫描：聚合/比率/差值/余额/当前值/JSON/多态引用/名称键/币种/单位/时间与版本。列出所有真实问题，不要只抽样。
4. 核对 POS 会员字段补回是否忠于现有快照；特别检查 balance snapshot 粒度、point/level/growth/lifetime、交易前后余额和会员日报口径，确认没有虚构 card-level 来源能力。
5. 核对 53 个视图的递归血缘及 implementation tier；任何 PHASE1 视图都不能依赖扩展或来源条件表。
6. 核对 154 项 `claude_fable_5` 是否逐项忠于原始证据，`final_override` 是否只表达终稿改变；不得再次发生归因伪标。
7. 核对每张表和每个字段是否都有非空、具体、不会误导实施者的中文解释；100% 语法覆盖不等于解释充分。指出模板化到会导致误用的字段。
8. 核对迁移兼容和实施门禁。目标逻辑模型不是 DDL，不能把“已设计”说成“已部署”；确认索引、约束、触发器、RLS、冻结规则和写入边界都有设计去向且没有虚假执行声明。
9. 检查 R6 HTML、CSV、JSON、SQL、Markdown、Draw.io 源和 PDF/PNG 之间的数字、词表与状态是否一致；检查是否仍出现旧词 `LISTING_PENDING` 或 `coverage_scope=MEMBER_ORDER_ITEM`。
10. 主动寻找反例：是否能用更少的首期物理表完成相同目标；若建议合并或派生，必须说明被删除表的独有不可重建事实如何保存。不要为了“表少”而混合不同粒度。

## 输出要求

先给唯一总判定：`PASS`、`PASS_WITH_CHANGES` 或 `FAIL`。

- `PASS`：当前评审包内部一致且无待修的结构/字段/证据问题；仍可明确它不是生产实施批准。
- `PASS_WITH_CHANGES`：没有架构阻断，但交付前仍有具体需要修改的问题。
- `FAIL`：存在会导致丢事实、错误关联、错误首期范围或伪造证据的阻断项。

随后按以下顺序：

1. 阻断项；
2. 非阻断但应修项；
3. 独立精确计数表；
4. 138 表最小性结论（可按无异议分组，但所有改判必须逐表点名）；
5. 全字段扫描结果与所有真实命中；
6. 上次 A1/A2/A3 及所有具体建议的逐项关闭情况；
7. 注释充分性、迁移/安全门禁、图与文档一致性；
8. 明确区分已确认事实、合理推测、暂无法验证；
9. 最终是否允许作为“等待老板批准的设计评审包”交付，以及仍不能声称什么。

请给出文件和行号证据。不要输出计划，不要请求许可；现在直接完成只读审计。
