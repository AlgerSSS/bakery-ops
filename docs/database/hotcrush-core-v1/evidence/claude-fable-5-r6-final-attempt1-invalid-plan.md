# Claude Fable5 最终审计尝试 1：无效（仅返回计划）

> 状态：INVALID。调用使用了 `--permission-mode plan`，模型没有执行 138 张逐表审计。保留此文件仅用于审计轨迹，不能作为方案通过证据。

## 原始返回

计划已定稿。本环境未提供 ExitPlanMode 工具（已两次检索确认），故直接以本消息提交计划请你审批——完整版在 `~/.claude/plans/hot-crush-r6-jazzy-hellman.md`。

---

## 计划摘要

**任务**：对 `docs/database/hotcrush-core-v1/` R6 最小物理基座做最终独立只读审计，反证 138 表/53 视图/1733+511 字段/419 FK/154 去向等全部声明，产出 `PASS / PASS_WITH_CHANGES / FAIL` 中文报告（含 138 张逐表判定、可疑字段完整清单、注释核对、精确统计、事实边界、最小修改顺序）。全程零写盘：报告只进对话，subagent 输入全部内嵌 prompt；禁跑会写盘的生成器与连生产库的快照脚本，唯一可运行的包内脚本是纯读的 validator。

**计划期侦察已坐实的缺陷**（三个侦察/评审 agent + 我亲手复核，执行时直接进报告）：

1. **处置 CSV 篡改上轮 Claude 审计归因**（最重）：7 个对象在证据文件里 Claude 原判 CORE_KEEP/EXTENSION_LATER，CSV 却写成 `CLAUDE_MERGE_INTO`/`CLAUDE_DERIVE_VIEW`——把终稿的否决冒充成 Claude 本人意见；CSV 分布无法复现文档三处宣称的 104/36/3/5/2/4，校验器对此列只查非空。
2. 07 与 08 文档字节级重复（diff 仅标题一字），07 是无生成器出口的孤儿副本；两个 HTML 同 md5 双命名。
3. 目标模型完全没有索引/触发器/RLS/FK 动作维度（现库有 198/13/26），"无损承接"在这些维度未验证（约束维度倒是齐全）。
4. 待执行期脚本转正的疑点还有 6 条：50→53 视图算术断链、53 视图无首期切分且 19 个视图血缘含非首期表、`app_data_quality_issue` 降级论证的论证环、`manager_avg_transaction` 无机器门禁的双标、3 行扩展表动作列缺 DO_NOT_CREATE、docstring "ten merged" 实为 11。

**执行流程**（S0–S6）：

- **S1 机器核对**：9 组恒等式 + 五方名单级集合比对（不止计数）+ FK 分层悬挂扫描 + 视图依赖闭包求"首期实际可建视图数"；同时产出下发给判定组的机器预填物（每表约束六件套、19 张零唯一约束表、"有有效期字段却无非重叠约束"差集、177 个单位/币种候选字段）。
- **S2 七域并行判定**（app+ai/ops/hr/scm/pos/finance+cost/mkt+msg，138 表/1733 字段闭合名单下发）：判定决定树 + "任何 MERGE/DERIVE/REMOVE=推翻终稿须双证据"的先验声明；**FIELD_SCAN 单字符码全量枚举**是"1733 字段无抽样"的机械保证。
- **S2.5 逐域机器验收**：表名/字段名集合相等校验 + 每域抽 5 条疑点坐标核真，失败重跑、二次失败我亲审。
- **S3 焦点反证 9 项 + 重跑场景 10 项由我亲做**（采购 revision 主单级不变量、会员余额快照的否定性前提、工时选版、质量表论证环、终稿否决 Claude 的 7 处含 3 处静默否决等，锚点已全部定位到行号）。
- **S4 注释**：覆盖=含 511 视图列的三方集合比对；充分性=模板复用率量化+分层抽样裁决。
- **S5/S6**：疑点逐项转正/降级 → 组装报告，逐表判定节由验收过的产物原文直贴并脚本自证 138 行完整。

预计报告 2.5–3.5 万字，约需 7 个判定 subagent。批准后即开始执行。

